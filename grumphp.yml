parameters:
    git_dir: .
    bin_dir: ./vendor/bin
    tasks:
        composer: ~
        phpcs:
            standard: "phpcs.xml"
        securitychecker: ~
        jsonlint: ~
        git_blacklist:
            triggered_by: [php]
            keywords:
                - "die("
                - "var_dump("
                - "dump("
                - "exit;"
