{% for key, author in event.eventable.authors %}
    <a href="{{ url('users.show', author.identifier) }}">{{ author.display_name }}</a>{% if (key + 1) < event.eventable.authors.count() %},{% endif %}
{% endfor %}
 created <a href="{{ url('requests.show', event.eventable.identifier) }}">{{ event.eventable.name }}</a>
