{% extends 'layout.twig' %}
{% set title = user.name %}

{% block content %}
    <table class="table table-condensed table-hover table-striped">
        <thead>
            <tr>
                <th>RFC</th>
                <th>Voted</th>
                <th>Approval %</th>
            </tr>
        </thead>
        {% for vote in user.votes %}
            <tr>
                <td><a target="_blank" href="{{ vote.request.link }}">{{ vote.request.name }}</a></td>
                <td>{{ vote.vote ? 'Yes' : 'No' }}</td>
                <td>{{ vote.request.approval * 100 }}%</td>
            </tr>
        {% endfor %}
    </table>
{% endblock %}
